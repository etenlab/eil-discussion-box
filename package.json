{
  "name": "eil-discussion-box",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "lerna": "lerna",
    "dev": "lerna run start --stream --parallel",
    "test": "lerna run test --",
    "build": "lerna run build",
    "prepublish": "lerna run prepublish"
  },
  "devDependencies": {
    "lerna": "^6.0.1"
  }
}
